import { IconHome } from 'asset/icon';

const Icons = () => {
    return (
        <>
            <IconHome size={30} color={'red'} />
            <IconHome size={30} color={'point'} />
        </>
    );
};

export default Icons;
